<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="action_account_invoices_no_amount" model="ir.actions.report">
            <field name="name">Invoice without Amount</field>
            <field name="model">account.move</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">account_invoice_no_amount.report_invoice_no_amount</field>
            <field name="report_file">account_invoice_no_amount.report_invoice_no_amount</field>
            <field name="print_report_name">(object._get_report_base_filename())</field>
            <field name="attachment"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_type">report</field>
        </record>

    </data>
</odoo>
